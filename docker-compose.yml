services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./backend:/app
      - chroma_data:/app/chroma_data
    env_file:
      - ./backend/.env
volumes:
  chroma_data:
